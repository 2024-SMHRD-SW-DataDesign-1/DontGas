async function getOdorNow(phouseId) {
            

            try {
                
				
                
            } catch (error) {
                console.error('Error fetching the data:', error);
            }
        }

        // Call the function to get weather data when the page loads
        getOdorNow(phouse);